@model IEnumerable<AmberWatch.Areas.Customer.Models.WatchModel>

@{
    Layout = "~/Views/Shared/_Layout.cshtml"; // Nếu có sử dụng Layout
}
<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css"> <!-- Đường dẫn đến Bootstrap -->
    <link rel="stylesheet" href="~/lib/font-awesome/css/all.min.css"> <!-- Đường dẫn đến Font Awesome nếu cần -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">

</head>

<body>
    <div class="container mt-4" style="margin-top: 70px !important;" >
        <a href="javascript:history.back()">
            <i class="bi bi-arrow-left-short" style="color: rgb(0, 0, 0); font-size: 40px;"></i>
        </a>
        <h2>Danh sách đồng Orient</h2>
        <div class="row">
            @foreach (var watch in Model)
            {
                var discountedPrice = watch.price * 0.9; // Giá sau khi giảm 10%

                <div class="col-md-3 mb-4">
                    <a href="@Url.Action("Detail", "Watch", new { id = watch.id_watch})"
                        class="text-decoration-none text-dark">
                        <div class="card h-100 shadow-sm border-0">
                            <div class="position-relative">
                                <img src="@watch.img" class="card-img-top mx-auto d-block"
                                    style="max-width: 80%; padding: 10px;" alt="@watch.name">
                                <span class="badge bg-danger position-absolute top-0 end-0 m-2">-10%</span>
                            </div>
                            <div class="card-body text-center">
                                <div style="width: 50%;background-color: aliceblue;border-radius: 10px;">
                                    <p class="text-start" style="font-size: 15px; margin-left: 5px;"> ❤️ @watch.sex</p>
                                    <p class="text-start" style="font-size: 15px; margin-left: 5px;"> 🔖 @watch.model</p>
                                </div>
                                <div>
                                    <p class="text-center">@watch.name</p>
                                </div>
                                <div style="display:flex; justify-content: center;" class="text-center">
                                    <p class="text-decoration-line-through text-muted">@($"{watch.price:0,0}₫")</p>
                                    <p class="fw-bold" style="margin-left: 15px;">@($"{discountedPrice:0,0}₫")</p>
                                </div>

                            </div>
                        </div>
                    </a>


                </div>
            }
        </div>
    </div>
</body>
